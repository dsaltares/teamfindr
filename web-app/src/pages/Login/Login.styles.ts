import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  root: {
    minHeight: '100vh',
  },
  buttonsContainer: {
    maxWidth: '300px',
  },
  buttonContainer: {
    width: '100%',
  },
}));

export default useStyles;
