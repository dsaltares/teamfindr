import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  navigation: {
    position: 'fixed',
    bottom: 0,
    width: '100%',
  },
}));

export default useStyles;
